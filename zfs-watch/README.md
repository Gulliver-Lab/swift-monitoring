# zfs-watch

Python package to find who should own the data (tracked by zfs)
of past users (whose accounts expired)

To install/run:
```bash
uv venv
uv pip install "git+https://github.com/Gulliver-Lab/swift-monitoring#subdirectory=zfs-watch"
uv run track-expired
```
